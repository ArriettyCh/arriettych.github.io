---
// Hero Section
---

<section id="hero" class="hero">
  <div class="hero-shell">
    <div class="hero-intro fade-in-up">
      <p class="hero-kicker">Research · Product · Taste</p>
      <h1 class="hero-title">
        网站仅作排版示意，内容与任何人无关。
        
      </h1>
      <p class="hero-lead">
        我关注模型能力与产品体验之间的转换过程：从算法边界、系统落地到交互表达。这里记录我正在构建的项目、持续研究的问题，以及对“好产品”判断标准的演化。
      </p>
    </div>

    <div class="hero-stack fade-in-up">
      <article class="hero-note">
        <h3>Current Focus</h3>
        <div class="prose">
          <p>生成模型与 Agent 系统在真实场景下的可用性与可持续迭代。</p>
          <p>把研究结论转成可以验证、可以复用、可以被用户感知的产品能力。</p>
        </div>
      </article>

      <article class="hero-note">
        <h3>What You Will See</h3>
        <div class="prose">
          <p>精选项目拆解：问题定义、关键取舍、以及最终带来的用户价值。</p>
          <p>阶段性思考与实验记录：关注趋势，更关注可落地的方法。</p>
        </div>
      </article>
    </div>

  </div>
</section>

<style>
  .hero {
    position: relative;
    isolation: isolate;
    overflow: clip;
    min-height: 100svh;
    padding-top: 0;
    padding-bottom: var(--space-xl);
  }

  @supports not (height: 100svh) {
    .hero {
      min-height: 100vh;
    }
  }

  .hero-shell {
    position: relative;
    display: grid;
    gap: var(--space-lg);
    width: 100%;
    align-content: start;
    padding-top: calc(var(--header-height) + var(--space-sm));
  }

  .hero-intro {
    display: grid;
    gap: var(--space-md);
    max-width: 100%;
    padding: 0;
  }

  .hero-kicker {
    font-size: 0.76rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
    font-weight: var(--font-weight-ui);
  }

  .hero-title {
    font-size: clamp(2.35rem, 5.6vw, 5.2rem);
    line-height: 1.03;
    letter-spacing: -0.02em;
    max-width: 100%;
    text-wrap: balance;
    color: var(--color-text);
  }

  .hero-lead {
    max-width: 100%;
    font-size: clamp(1.04rem, 1.22vw, 1.24rem);
    line-height: 1.66;
    color: var(--color-text-muted);
  }

  .hero-stack {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: var(--space-md);
    align-items: stretch;
    max-width: 100%;
  }

  .hero-note {
    display: grid;
    grid-template-rows: auto 1fr;
    gap: 0.7rem;
    padding: 0;
    height: 100%;
  }

  .hero-note h3 {
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text);
    padding-bottom: 0.45rem;
    border-bottom: 1px solid var(--color-border);
  }

  .prose p {
    margin-bottom: 0.8rem;
    font-size: clamp(1.02rem, 1.1vw, 1.16rem);
    line-height: 1.64;
    color: var(--color-text-muted);
  }

  .prose p:last-child {
    margin-bottom: 0;
  }

  .hero-scroll-cue {
    font-size: 0.72rem;
    font-family: var(--font-mono);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  @media (max-width: 768px) {
    .hero {
      padding-bottom: var(--space-lg);
    }

    .hero-shell {
      padding-top: calc(var(--header-height) + var(--space-sm));
      gap: var(--space-md);
    }

  }
</style>
