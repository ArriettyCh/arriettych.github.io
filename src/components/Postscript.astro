---
// Postscript Tailnote (Failure Museum)
---

<section id="postscript" class="fade-in-up">
  <div class="postscript-shell">
    <div class="postscript-meta">
      <p class="label">Postscript</p>
      <p class="postscript-series">失败博物馆 01</p>
    </div>

    <div class="postscript-note">
      <h3>有些失败不该被藏起来，它更像精选案例后的脚注。</h3>
      <p>
        我曾尝试做一个校园外卖拼单产品。最初以为难点会是需求匹配算法与高并发，后来才意识到，真正让它无法成立的，是信任建立、支付清算，以及供给侧冷启动这三个更底层的问题。
      </p>
      <p>
        这件事之后，我开始更警惕“把可实现误判成可成立”。真实世界里，决定产品能不能活下来的，往往不是功能是否做出来，而是用户为什么会信、会用、会留下来。
      </p>
    </div>
  </div>
</section>

<style>
  #postscript {
    padding-top: var(--space-sm);
    padding-bottom: var(--space-lg);
  }

  .postscript-shell {
    display: grid;
    grid-template-columns: minmax(10rem, 14rem) minmax(0, 1fr);
    gap: var(--space-md);
    align-items: start;
    max-width: 1040px;
    margin-left: auto;
    padding-top: var(--space-sm);
    border-top: 1px solid color-mix(in srgb, var(--color-border) 75%, transparent);
  }

  .postscript-meta {
    display: grid;
    gap: 0.4rem;
  }

  .postscript-series {
    font-size: 0.85rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-text-muted);
  }

  .postscript-note {
    position: relative;
    display: grid;
    gap: var(--space-sm);
    padding-left: var(--space-md);
  }

  .postscript-note::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0.3rem;
    bottom: 0.3rem;
    width: 1px;
    background: linear-gradient(
      to bottom,
      color-mix(in srgb, var(--color-text) 65%, transparent),
      color-mix(in srgb, var(--color-border) 80%, transparent)
    );
  }

  .postscript-note h3 {
    max-width: 28ch;
    font-size: clamp(1.35rem, 1.8vw, 1.8rem);
    line-height: 1.25;
  }

  .postscript-note p {
    max-width: 62ch;
  }

  @media (max-width: 1023px) {
    .postscript-shell {
      grid-template-columns: 1fr;
      gap: var(--space-sm);
    }

    .postscript-note {
      padding-left: var(--space-sm);
    }
  }
</style>
